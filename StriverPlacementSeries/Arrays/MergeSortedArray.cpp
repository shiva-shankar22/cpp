#include<bits/stdc++.h>
using namespace std;

//TC -- O(M*N)
void mergeArrays(vector<int> &a , vector<int> &ar){
    int n=a.size(),m=ar.size();
     int i=0,j=0;
    
    for(int i=0;i<n;i++){
         if(a[i]>ar[j])swap(a[i],ar[j]); 

         int val=ar[j];

         

    }
   
}

int main(){
      
}